pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{go}"
      run: |
        golangci-lint run {staged_files} --fix
      stage_fixed: true

post-merge:
  commands:
    npm:
      glob: "{go.mod,go.sum}"
      run: go mod download
